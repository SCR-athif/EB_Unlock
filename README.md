# EB_Unlock
A tool to crack the password protected Files using automation

## Steps to use

1. Clone the script file
2. Add executable Permission `Chmod +x ebunlock`
3. Move into the bin directory `sudo mv ebunlock /usr/bin/ebunlock`
4. run by typing `ebunlock`

